# GPT考核驾驶舱 - 项目知识索引图谱

> 本文档为项目的完整知识索引，包含文件结构、数据流向、函数依赖、配置变量、数据库 Schema 等关键知识。

---

## 一、项目概览

| 项目名 | GPT考核驾驶舱 |
|--------|----------------|
| 仓库 | `xiaochen05281/gpt-dashboard` |
| Pages 地址 | `https://xiaochen05281.github.io/gpt-dashboard/` |
| 主文件 | `GPT-Assessment-Dashboard.html` |
| 技术栈 | HTML + CSS + JS（单文件应用）、Dexie.js（IndexeDB）、ECharts、XLSX、Firebase Firestore |

---

## 二、文件结构索引

```
5月数据明细/
├── GPT-Assessment-Dashboard.html   ← 主应用（~4500行，所有逻辑在此）
├── index.html                        ← Pages 入口（自动跳转到主文件）
├── push.bat                         ← 一键推送脚本（git add + commit + push）
├── 中心名单模板.xlsx                 ← 中心名单模板文件
├── .github/
│   └── workflows/
│       └── update-data.yml          ← GitHub Actions 自动更新配置
├── source/                         ← Excel 数据源文件
│   ├── 1、工时过高/               ← 工时过高数据（5月/6月）
│   ├── 2、工时偏低/
│   ├── 3、工资偏高/
│   ├── 4、后勤及职能占比/
│   ├── 5、主管组长管幅/
│   ├── 6、连续出勤异常/
│   ├── 7、连续未出勤/
│   ├── 8、绩效异常岗位/           ← 子目录：装车岗/卸车岗/倒包岗/供件岗/封包岗/分拣岗/扫描岗
│   ├── 9、人数基数/
│   ├── 中心名单-2025-05.xlsx
│   └── 中心名单-2025-06.xlsx
├── scripts/
│   ├── excel_to_json.py           ← Excel 转 JSON 脚本（用于 CI）
│   └── GPT-Assessment-Dashboard-deploy.html  ← 部署测试版（已弃用）
└── .workbuddy/
    └── memory/
        └── 2026-06-03.md       ← 工作记忆日志
```

---

## 三、数据流向图

```
Excel 文件（source/ 目录）
        │
        ▼
 ┌─────────────────────────────┐
 │  方式A：浏览器手动上传    │
 │  handleSlotUpload()        │
 │  handleCenterListFile()    │
 └─────────────┬───────────┘
                   │
                   ▼
 ┌─────────────────────────────┐
 │  方式B：自动加载（初始化） │
 │  REPO_FILE_LIST            │
 │  loadDefaultDataFiles()     │
 └─────────────┬───────────┘
                   │
                   ▼
 ┌─────────────────────────────┐
 │  parseExcelFile()          │
 │  → XLSX.read()           │
 │  → sheet_to_json()        │
 │  → normalizeDataCenters() │
 └─────────────┬───────────┘
                   │
                   ▼
 ┌─────────────────────────────┐
 │  storeData()              │
 │  → Dexie.js             │
 │  → IndexedDB             │
 │  (按 month 分月存储)    │
 └─────────────┬───────────┘
                   │
                   ▼
 ┌─────────────────────────────┐
 │  calculateAllStats()      │
 │  → 从 IndexedDB 读取   │
 │  → 计算各维度统计        │
 └─────────────┬───────────┘
                   │
                   ▼
 ┌─────────────────────────────┐
 │  renderDashboard()       │
 │  renderOnlineDetailTable()│
 │  renderOfflineDetailTable │
 │  renderRegionDetailTable()│
 │  renderProvinceTable()   │
 │  renderTrendCharts()     │
 └─────────────────────────────┘
```

---

## 四、配置变量清单

### 4.1 月份配置（必须随数据更新而修改）

| 变量名 | 位置（行号约） | 说明 | 示例值 |
|--------|----------------|------|---------|
| `DATA_MONTH_LABEL` | ~4110 | 文件名中的月份标识 | `'6月'` |
| `DATA_MONTH` | ~4111 | 数据库存储的月份格式 | `'2026-06'` |

**修改方法：** 换月份时，改这两个变量即可。
```javascript
const DATA_MONTH_LABEL = '7月';      // 文件名中的月份标识
const DATA_MONTH = '2026-07';       // 数据库存储的月份格式
```

### 4.2 Firebase 配置

| 变量名 | 位置 | 说明 |
|--------|------|------|
| `FIREBASE_CONFIG` | ~1303 | Firebase 配置对象 |
| `FIREBASE_ENABLED` | ~1311 | 是否启用 Firebase（`apiKey !== ""` 时启用） |

### 4.3 大区划分配置

| 变量名 | 位置 | 说明 |
|--------|------|------|
| `PROVINCE_TO_REGION` | ~1622 | 省区 → 大区 映射表 |
| `PROVINCE_NAME_MAP` | ~1622 | 省区名称标准化映射 |

**当前大区划分：**
- 东北区：辽宁省区、吉林省区、黑龙江省区、内蒙古区、内蒙古省区
- 华北区：北京区、天津区、河北省区、山西省区
- 华东区：上海区、江苏省区、浙江省区、安徽省区、福建省区、江西省区
- 华南区：广东省区、广西省区、海南省区、湖南省区、湖北省区
- 西南区：重庆市区、四川省区、贵州省区、云南省区、西藏自治区区、青海省区
- 西北区：陕西省区、甘肃省区、宁夏回族自治区区、新疆维吾尔自治区区
- 中原区：山东省区、河南省区、江苏省区（北部）、安徽省区（北部）

### 4.4 中心类型配置

| 变量名 | 位置 | 说明 |
|--------|------|------|
| `DEFAULT_ONLINE_CENTERS` | ~1689 | 默认线上中心列表 |
| `centerTypes` | ~1965 | 当前激活的中心类型映射（`{中心名: 'online'/'offline'}`） |
| `currentCenterTypesMonth` | ~1966 | 当前使用的中心名单月份 |

**多月份存储结构（`db.settings` 中 `key='centerTypesMap'`）：**
```javascript
{
  '2026-05': { '广州': 'online', '北京': 'offline', ... },
  '2026-06': { '广州': 'online', '北京': 'offline', ... }
}
```

### 4.5 考核规则配置

| 变量名 | 位置 | 说明 |
|--------|------|------|
| `DEFAULT_SCORING_CONFIG` | ~1701 | 默认考核规则（目标值、权重等） |
| `VALID_LEAVE_STATUSES` | ~2023 | 有效请假状态（不含"销假成功"） |

---

## 五、数据库 Schema（IndexeDB via Dexie.js）

数据库名：`GPTAssessmentDB_v1`

### 5.1 数据表（按月份分月存储）

| 表名 | 索引 | 说明 | 关键字段 |
|------|-------|------|----------|
| `headcount` | `++id, center, month, province, [center+month]` | 人数基数 | `month, center, province, performanceType, headcount, monthEndHeadcount` |
| `performance` | `++id, center, month, province, job, [center+month]` | 绩效异常（按岗位分） | `month, center, province, job, target, avgPerformance, deviation` |
| `salary` | `++id, center, month, province, [center+month]` | 工资偏高人员 | `month, empId, name, job, center, province, salary, avgSalary, deviation` |
| `attendance` | `++id, center, month, province, [center+month]` | 连续出勤异常 | `month, center, province, consecutiveDays` |
| `absence` | `++id, center, month, province, [center+month]` | 连续未出勤 | `month, center, province, consecutiveDays` |
| `overtime` | `++id, center, month, province, [center+month]` | 工时过高 | `month, center, province, avgOvertime` |
| `undertime` | `++id, center, month, province, [center+month]` | 工时偏低 | `month, center, province, avgUndertime` |
| `managerSpan` | `++id, center, month, [center+month]` | 主管组长管幅 | `month, center, province, manager, span` |
| `logisticsRatio` | `++id, center, month, [center+month]` | 后勤及职能占比 | `month, center, province, logisticsRatio` |
| `leaveRecord` | `++id, center, month, empId, [center+month], [empId+month]` | 请假记录 | `month, empId, name, center, province, leaveType, startDate, endDate, status` |

### 5.2 设置表

| 表名 | 键 | 值说明 |
|------|-----|---------|
| `settings` | `centerTypesMap` | 多月份中心类型映射（JSON） |
| `settings` | `centerTypes` | 单一中心类型映射（兼容旧格式） |
| `settings` | `provinceToRegion` | 省区→大区映射（用户自定义） |
| `cachedStats` | `++id, month` | 缓存的统计结果 |

---

## 六、函数依赖关系

### 6.1 初始化流程

```
init()
  ├── updateSyncIndicator()
  ├── loadSettings()
  │   ├── db.settings.get('provinceToRegion')
  │   ├── getCenterTypesMap()
  │   ├── loadCenterTypesForMonth()
  │   └── 兼容旧格式迁移
  ├── updateDataStatus()
  ├── updateMonthSelectors()
  │   └── getAllDataMonths()
  ├── initEventHandlers()
  │   ├── 上传 Slot 事件绑定
  │   ├── 中心名单上传事件绑定
  │   ├── 设置保存事件绑定
  │   └── 月份切换事件绑定
  ├── renderSettings()
  ├── updateCenterListAllStatus()
  └── loadStatsForMonth(currentMonth)
      ├── loadCenterTypesForMonth(month)   ← 自动切换中心名单
      ├── calculateAllStats(month)
      ├── calculateAllStats(prevMonth)
      ├── renderDashboard(stats, prevStats)
      ├── renderOnlineDetailTable()
      └── renderOfflineDetailTable()
```

### 6.2 文件上传流程

```
handleSlotUpload(file, type, job)
  ├── 从文件名提取月份（extractMonthFromFilename）
  ├── 从文件内容提取月份（normalizeMonth）
  ├── parseExcelFile(file)
  │   └── XLSX.read() → sheet_to_json()
  ├── normalizeDataCenters(data)
  ├── storeData(type, data, job)
  │   └── db[type].bulkPut()
  └── updateDataStatus()

handleCenterListFile(file)
  ├── extractMonthFromFilename(file.name)
  ├── 或读取 center-list-month-input 的值
  ├── parseExcelFile(file)
  ├── 解析中心类型（线上/非线上）
  ├── saveCenterTypesForMonth(month, types)
  │   └── db.settings.put({ key: 'centerTypesMap', value: map })
  └── saveSettingsToFirestore()
```

### 6.3 统计计算流程

```
calculateAllStats(month)
  ├── 从各表读取数据（db[type].where({ month }).toArray()）
  ├── 按大区聚合（PROVINCE_TO_REGION）
  ├── 按线上/非线上分类（centerTypes）
  ├── 计算各维度指标
  │   ├── 人数基数 → headcount
  │   ├── 绩效异常 → performance
  │   ├── 工资偏高 → salary
  │   ├── 出勤异常 → attendance / absence
  │   ├── 工时异常 → overtime / undertime
  │   ├── 管幅异常 → managerSpan
  │   └── 后勤占比 → logisticsRatio
  └── 返回 stats 对象
```

---

## 七、关键业务逻辑

### 7.1 多月份数据共存

**实现方式：**
1. 所有数据表都有 `month` 字段
2. `storeData()` 只删除当前月份+当前岗位的数据（不删其他月份）
3. `loadMonthSelectors()` 从所有表收集有数据的月份，生成下拉框
4. 切换月份时，重新计算并渲染对应月份的数据

**中心名单多月份：**
- 使用 `centerTypesMap` 存储结构：`{ '2026-05': {...}, '2026-06': {...} }`
- 切换数据月份时，自动加载对应月份的中心名单
- 兼容旧格式：首次加载时自动迁移

### 7.2 大区划分

**映射表：** `PROVINCE_TO_REGION`（~1622 行）

**修改方法：**
1. 直接修改 `PROVINCE_TO_REGION` 对象
2. 推送代码后生效

**当前特殊调整：**
- 内蒙古区：从华北区调整到东北区（2026-06-03 修改）

### 7.3 中心类型（线上/非线上）

**判断逻辑：**
1. 优先使用上传的中心名单（`centerTypesMap`）
2. 名单中没有的中心 → 默认按**非线上**处理
3. 未上传任何名单 → 使用 `DEFAULT_ONLINE_CENTERS` 默认列表

---

## 八、GitHub Actions 自动化

### 8.1 触发条件

`.github/workflows/update-data.yml`：

```yaml
on:
  schedule:
    - cron: '0 0 * * *'    # 每天北京时间早上8点（UTC 00:00）
  workflow_dispatch:       # 允许手动触发
  push:
    paths:
      - 'data/**.xlsx'
      - 'data/**.xls'
```

### 8.2 执行流程

```
触发 → checkout code → setup python → pip install pandas openpyxl
  → python excel_to_json.py
  → git add data/*.json → git commit → git push
```

### 8.3 注意事项

- **公司网络会拦截 GitHub HTTPS（443端口）** → 推送需连手机热点
- **salary.json 超过 100MB** → 已从 Git 历史中移除，改用 Excel 源文件直接部署

---

## 九、常见操作指南

### 9.1 更新数据（新月份）

1. 准备新月份的 Excel 文件，命名为 `XXX-2026-07.xlsx`
2. 放入对应的 `source/` 子目录
3. 修改 `GPT-Assessment-Dashboard.html` 中的：
   ```javascript
   const DATA_MONTH_LABEL = '7月';
   const DATA_MONTH = '2026-07';
   ```
4. 连手机热点，双击 `push.bat` 推送

### 9.2 修改大区划分

1. 修改 `PROVINCE_TO_REGION` 对象（~1622 行）
2. 推送代码

### 9.3 修改考核规则

1. 在网页"设置"页 → "考核规则配置" 中修改
2. 点击"保存规则配置"
3. 会自动保存到 `db.settings`（键：`scoringConfig`）

---

## 十、已知问题 & TODO

### 10.1 已修复

- [x] `storeData()` 致命 Bug：`await table.clear()` 每次上传清空整张表 → 改为按月份+岗位删除
- [x] `VALID_LEAVE_STATUSES` 包含"销假成功" → 已移除
- [x] `slice(1)` 导致第一条数据（合肥）被跳过 → 已移除
- [x] 中文文件名加载失败（encodeURI 问题） → 已移除 encodeURI
- [x] 内蒙古区划分错误（在华北区） → 已调整到东北区

### 10.2 待验证

- [ ] 刷新 Pages 页面，确认内蒙古区显示在东北区
- [ ] 测试中心名单多月份上传（5月+6月各上传一份）
- [ ] 验证切换数据月份时中心名单自动同步切换

---

## 十一、联系人 & 权限

| 角色 | 说明 |
|------|------|
| 仓库 Owner | `xiaochen05281` |
| 部署方式 | GitHub Pages（从 `main` 分支部署） |
| 本地分支 | `master` |
| 远程分支 | `main` |
| 推送命令 | `git push origin master:main` |

---

## 十二、快速参考

### 12.1 一键推送脚本（push.bat）

```bat
@echo off
cd /d "%~dp0"
git add -A
git commit -m "update: %date% %time%"
git push origin master:main
pause
```

### 12.2 常用 Git 命令

```bash
# 查看状态
git status

# 提交并推送
git add -A
git commit -m "feat: xxx"
git push origin master:main

# 创建备份分支
git checkout -b backup-2026-05
git checkout master
```

---

*最后更新：2026-06-03*
*更新人：AI Assistant*
