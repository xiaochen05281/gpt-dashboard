## 2026-06-03 工作记录

### 已完成
- [x] 调整大区划分：内蒙古区从华北区移到东北区（PROVINCE_TO_REGION 映射表）
- [x] 修复 `push.bat` 编码问题（UTF-8 设置导致 CMD 乱码，改为纯英文极简版本）
- [x] 成功推送大区调整修改到 GitHub（提交 22aad62）

### push.bat 当前状态
- 位置：`C:\Users\admin\Desktop\AI工具\智多星改善\5月数据明细\push.bat`
- 功能：双击自动执行 git add → commit → push
- 注意事项：公司网络会拦截 GitHub，推送需先连手机热点

### 新增完成
- [x] 中心名单上传改造为多月份支持
  - 添加 `centerTypesMap` 存储结构：`{ '2026-05': {...}, '2026-06': {...} }`
  - HTML 添加"名单月份"下拉选择框（`center-list-month-select`）
  - `handleCenterListFile()` 上传时按选择月份存储
  - `loadCenterTypesForMonth()` 切换当前使用的中心名单
  - `loadStatsForMonth()` 自动切换到数据对应月份的中心名单
  - 加载默认名单、自动加载、设置保存均适配多月份
  - 兼容旧格式：`centerTypes` 单一键值自动迁移到多月份格式
  - Firestore 同步改为保存/加载 `centerTypesMap`

### Bug 修复
- [x] 修复中心名单下拉框不显示 2026-05 的问题
  - 原因：`refreshCenterMonthSelect()` 只从已有中心名单中读月份
  - 修复：新增 `getAllDataMonths()` 从所有数据表收集月份，下拉框显示所有有数据的月份
  - 已有中心名单的月份标 ✓，缺少的显示 "⚠️ 缺少中心名单"

### 新增完成（第二轮）
- [x] 中心名单月份选择改为输入框 + 文件名自动识别
  - 下拉框改为 `type="month"` 输入框（`center-list-month-input`）
  - 新增 `extractMonthFromFilename()` 从文件名提取月份（支持 2026-05、202605 等格式）
  - `handleCenterListFile()` 逻辑：文件名提取 → 输入框值 → 提示用户
  - "加载默认名单"按钮也改用输入框获取月份
  - 状态显示保留 `updateCenterListAllStatus()`，展示所有数据月份的中心名单情况

### 新增完成（第三轮）
- [x] 创建《陈先生个人档案.md》
  - 包含：基本信息、工作背景、技术能力评估、工作风格、项目经验、工作环境、学习路径建议、个人偏好、操作手册模板
  - 文件位置：`C:\Users\admin\Desktop\AI工具\智多星改善\5月数据明细\陈先生个人档案.md`
  - 同步更新到 `~/.workbuddy\USER.md`（每次新会话自动读取）

### 新增完成（第四轮）
- [x] 档案更名为"陈先生"（之前误写为"许先生"）
- [x] 在档案开头添加"使用须知"章节（AI 必须遵守）：
  1. 任何代码修改前必须先沟通，确认后再动手
  2. 任何 Git 推送前必须先沟通，告知变动内容
  3. 不确定时先问，不自己猜
  4. 有风险的命令必须先告知风险
  5. 更新内容必须记录到档案
- [x] 同步更新 `~/.workbuddy/USER.md`，确保每次新会话自动读取

### 验证待办
- [ ] 刷新 Pages 页面，确认内蒙古区显示在东北区
- [ ] 测试中心名单多月份上传（5月+6月各上传一份）
- [ ] 验证切换数据月份时中心名单自动同步切换
