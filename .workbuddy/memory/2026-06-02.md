# 2026-06-02 工作日志

## 已完成
- [x] 解决Git推送问题：公司网络防火墙拦截GitHub连接，换热点后成功
- [x] 解决合并冲突：远程main分支有旧文件，保留本地修复版本
- [x] 移除大文件：salary.json(130MB)等JSON数据文件超过GitHub 100MB限制，用git filter-branch从历史中移除
- [x] 成功推送到GitHub: `git push origin master:main --force`
- [x] 代码已上传到 https://github.com/xiaochen05281/gpt-dashboard

## 遇到的问题
1. 公司网络封了GitHub 443端口，Git Bash无法连接
2. 热点连接后Git Bash仍走有线网络（需确认WiFi图标或拔网线）
3. 远程main分支有旧文件导致合并冲突
4. data/json/下的JSON文件过大，GitHub拒绝推送

## 已完成（续）
- [x] 修复 `encodeURI` 导致的中文文件名加载失败问题（GitHub Pages 不编码中文路径）
- [x] 添加缺失的 `exportToExcel` 函数
- [x] 整理项目目录结构：新建 `source/`（数据源）、`scripts/`（工具脚本）
- [x] 清理无用文件：删除 apply_fixes.py、build_deploy.py、debug.log、.bak 备份
- [x] 修复 `.gitignore` 误忽略 Excel 文件的问题，确保数据源可部署
- [x] 成功推送 Excel 数据源（36.27 MiB）到 GitHub Pages

## 当前项目结构
```
5月数据明细/
├── .github/workflows/      # GitHub Actions
├── source/                 # Excel 数据源（1~10类 + 中心名单模板）
├── scripts/                # excel_to_json.py + deploy工具
├── GPT-Assessment-Dashboard.html  # 主程序
└── index.html              # Pages 入口
```

## 访问地址
- 仓库：https://github.com/xiaochen05281/gpt-dashboard
- Pages：https://xiaochen05281.github.io/gpt-dashboard/

## 已完成（续）
- [x] 修复 `index.html` 双文件维护问题：改为自动跳转到 `GPT-Assessment-Dashboard.html`
- [x] 修复遗漏的两个文件路径（请假+中心名单）：添加 `source/` 前缀并移除 `encodeURI`
- [x] 成功推送所有修复到远程（提交 b7569e7）

## 最终状态
- GitHub 仓库：https://github.com/xiaochen05281/gpt-dashboard
- Pages 部署地址：https://xiaochen05281.github.io/gpt-dashboard/
- 数据源：17 个 Excel 文件全部部署在 `source/` 目录下
- 自动加载：页面打开后自动读取 `source/` 下所有数据文件

## 注意事项
- 首次访问或更新后，可能需要 `Ctrl + Shift + R` 强制刷新清除缓存
- 公司网络会拦截 GitHub HTTPS 连接（443端口），推送需换手机热点
